<div class="pin-infobox-readonly-container" data-lat="{{lat}}" data-lng="{{lng}}" data-latLng="{{latLng}}">
  <h2 class="pin-infobox-readonly-title">{{name}}</h2>
  <div class="pin-trash-button-container">
    <button type="button" name="pin-trash-button" class="pin-trash-button"><i class="far fa-trash-alt"></i></button>
  </div>
</div>
